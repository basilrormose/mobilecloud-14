# If you needed to add database properties for the
# connection, you could put them here. This file
# is not used in the current example for the H2
# database.
spring.datasource.url=jdbc:h2:db/videodb;AUTO_SERVER=TRUE
spring.datasource.driverClassName=org.h2.Driver
spring.jpa.show-sql: true
spring.jpa.hibernate.ddl-auto=update
